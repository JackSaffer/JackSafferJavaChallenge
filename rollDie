public class question1 {
    public static void main (String[] args){
        //The code below creates a min and max value for the die rolls
        int min = 1;
        int max = 6;
        int range = max - min + 1 ;

        //This generates random numbers within 1 to 20
        for (int i = 1; i <= 20; i++){
            int rand = (int)(Math.random() * range) + min;
            //The output is going to be different everytime this is executed            
            System.out.println("Roll #" + i + " = " + rand);
        }
    }
}
